const t=document.getElementById('task');CONFIG.tasks.forEach(x=>t.add(new Option(x,x)));const p=document.getElementById('phases');CONFIG.phases.forEach((ph,i)=>p.innerHTML+=`<div class='phase'><h3>${ph.name} (${ph.weight}%)</h3>AI <select id='a${i}'>${CONFIG.ai.map((v,j)=>`<option value='${j}'>${v}</option>`).join('')}</select> Human <select id='r${i}'>${CONFIG.rw.map((v,j)=>`<option value='${j}'>${v}</option>`).join('')}</select></div>`);function calculate(){let total=0;let h='<h2>Calculation Details</h2><table><tr><th>Phase</th><th>Formula</th><th>Score</th></tr>';CONFIG.phases.forEach((ph,i)=>{let af=document.getElementById('a'+i).value/4;let rf=CONFIG.rwf[document.getElementById('r'+i).value];let s=ph.weight*af*rf;total+=s;h+=`<tr><td>${ph.name}</td><td>${ph.weight} × ${af.toFixed(2)} × ${rf.toFixed(2)}</td><td>${s.toFixed(2)}</td></tr>`});h+=`<tr><th colspan='2'>Total</th><th>${total.toFixed(2)}%</th></tr></table>`;document.getElementById('result').innerHTML='<h2>AI Contribution Score: '+total.toFixed(2)+'%</h2>';document.getElementById('details').innerHTML=h;}